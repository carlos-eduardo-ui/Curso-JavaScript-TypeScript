//string, number, undefined, null, boolean, symbol

const nome = 'luiz'; //string
const nome1 = "luiz"; //string
const nome2 = `luiz`;  //string
const num1 = 10; //number
const num2 = 10.53; //number
let nomeAluno;  //undefined -> (variavel nao aponta pra local nenhum na memoria [NULL]);
let sobrenomeAluno = null; //undefined -> (variavel nao aponta pra local nenhum na memoria [NULL]);
const aprovado = true; // Boolean = true, false (logico)

