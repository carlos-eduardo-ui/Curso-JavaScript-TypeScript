//escreva uma funcao chamada ePaisagem que recebe altura e largura de uma imagem e retorna true se a imagem estiver no modo paisagem 

const ePaisagem = (altura, largura) => largura >= altura;

const foto = ePaisagem(1920, 1920);

console.log(foto);
 
